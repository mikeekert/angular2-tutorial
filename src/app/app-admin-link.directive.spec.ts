import { AppAdminLinkDirective } from './app-admin-link.directive';

describe('AppAdminLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new AppAdminLinkDirective();
    expect(directive).toBeTruthy();
  });
});
